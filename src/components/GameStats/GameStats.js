import React, { Component } from 'react';

class GameStats extends Component {
  render() {
    return (
      <div className="">
          {this.props.gameId}
      </div>
    );
  }
}

export default GameStats;
